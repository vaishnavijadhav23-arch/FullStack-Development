spring.application.name=finance-advisor

# MongoDB Configuration - use env var in production
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://finance:finance@financeadvisor.p0euqix.mongodb.net/financeAdvisorDB?retryWrites=true&w=majority&appName=financeAdvisor}
spring.data.mongodb.database=financeAdvisorDB

# JWT Configuration - use env var in production
app.jwt.secret=${APP_JWT_SECRET:FinanceAdvisorSecretKey2024VeryLongSecretKeyForJWTTokenGeneration}
app.jwt.expiration=${APP_JWT_EXPIRATION:86400000}

# Server
server.port=${PORT:8080}

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000}

# Logging
logging.level.com.financeadvisor=INFO
